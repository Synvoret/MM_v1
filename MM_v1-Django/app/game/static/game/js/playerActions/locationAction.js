function locationAction(type_request) {

    if (type_request === 'location') {
        navLocationActions(type_request);
    };


    if (type_request === 'back') {
        navLocationActions(type_request);
    };

};