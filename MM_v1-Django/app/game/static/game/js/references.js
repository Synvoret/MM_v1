// GET REFERENCES for BOARD CONTAINER AREA
const boardContainer = document.getElementById('board-container');
const helperElement = boardContainer.querySelector('.helper');
const tspans = helperElement.querySelectorAll('tspan');
const textX = boardContainer.querySelectorAll('.text1');
const allElements = boardContainer.querySelectorAll('[name]');
const polygons = boardContainer.querySelectorAll('polygon[name]');
